import 'package:flutter/material.dart';

class AddShip extends StatelessWidget {
  const AddShip({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("اضافة شحنة"),
    );
  }
}
