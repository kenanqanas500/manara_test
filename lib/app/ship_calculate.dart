import 'package:flutter/material.dart';

class ShipCalculate extends StatelessWidget {
  const ShipCalculate({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("حاسبة الشحن"),
    );
  }
}
